import React from 'react'
import { Button } from './ui/button'

const MainNav = () => {
  return (
    <Button variant='ghost' className=' font-bold hover:text-orange-500 hover:bg-white text-xl'>Log in</Button>
  )
}

export default MainNav